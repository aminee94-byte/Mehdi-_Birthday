# Friend Museum photos

Put your friend photos in this folder, then reference them from `src/data/museumContent.ts`.

Example:

```ts
imageSrc: '/photos/birthday-photo.jpg'
```

You can use `.jpg`, `.png`, `.webp`, or `.gif` files. Keep file names simple, lowercase, and without spaces for easiest deployment.
